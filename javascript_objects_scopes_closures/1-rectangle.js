#!/usr/bin/node

class Rectangule {
    width;
    height;
      constructor(w, h) {
        w = this.width;
        h = this.height;
      }
  }
  
  const r1 = new Rectangle(2, 3);
  console.log(r1);
  console.log(r1.width);
  console.log(r1.height);